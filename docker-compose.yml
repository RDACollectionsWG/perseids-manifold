version: '2'
services:
  web:
    build: .
    ports:
     - "5000:5000"
    volumes:
     - .:/app
    links:
     - "marmotta"
  marmotta:
    image: "apache/marmotta:latest"
    #ports:
    # - "8080:8080"
    #expose:
    # - "8080"